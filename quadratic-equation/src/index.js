import React from 'react';
import ReactDOM from 'react-dom';
import QuadraticEquationComponent from './Contracts';
import './index.scss';

ReactDOM.render(
  <React.StrictMode>
    <QuadraticEquationComponent />
  </React.StrictMode>,
  document.getElementById('root')
);
